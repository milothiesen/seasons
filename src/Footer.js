import './Footer.css';
import React from 'react';

const Footer = () => (
    <div className='footer'>
        <p>
            This site reads a users location and determines whether they are in
            the northern or southern hemisphere and then returns an art object
            related to that season.
        </p>
    </div>
);

export default Footer;
